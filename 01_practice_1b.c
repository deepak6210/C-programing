#include<stdio.h>

int main()
{
    int length,breadth;
    printf("What is the length of rectange\n");
    scanf("%d", &length);
    printf("What is the breadth of rectange\n");
    scanf("%d", &breadth);
    printf("The area of your rectangle %d",length*breadth);
    return 0;

}