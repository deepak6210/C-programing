#include<stdio.h>

int main()
{
    // int a ; b = a; // it is invalid becuse b is not defined by us to the compiler 
    int v = 3^3;  // this is bitwise zor operator we don't get the power of the but yes it is valid 
    printf("this is valid this 3^3 %d\n", v);
    // char dt = '21 dec 2020'; // this is not valid because char contain a space of single digit so we get error 
    return 0;

}